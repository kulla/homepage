## Project Overview

This is a personal homepage built with handwritten HTML and CSS which currently
serves a CV for a fullstack software developer. All files can be found in `src`.
The project uses Bun as the runtime and build tool, with TypeScript support and
Prettier for formatting.
