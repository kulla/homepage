{
  "name": "resume",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "format": "prettier --write .",
    "lint:format": "prettier --check ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.7.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
